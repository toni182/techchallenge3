auth-service:
  namespace: auth-service
  maxSurge: 50%
  maxUnavailable: 0
  replicas: 2
  image: 654654467270.dkr.ecr.us-east-1.amazonaws.com/auth-service:7512dbf
  port: 8001
  ingress: 
    path: /auth-service(/|$)(.*)

flag-service:
  namespace: flag-service
  maxSurge: 50%
  maxUnavailable: 0
  replicas: 2
  image: 654654467270.dkr.ecr.us-east-1.amazonaws.com/flag-service:e8c9342
  port: 8002
  ingress: 
    path: /flag-service(/|$)(.*)
  configMap:
    name: db-flag-service-init-sql
    init.sql: |
      CREATE TABLE IF NOT EXISTS flags (
          id SERIAL PRIMARY KEY,

          -- 'name' é a chave de negócio única (ex: 'enable-new-checkout')
          name VARCHAR(100) UNIQUE NOT NULL, 
          
          description TEXT,
          
          -- Este é o 'kill switch' global. Se for false, a flag está desativada para todos.
          is_enabled BOOLEAN NOT NULL DEFAULT false,
          
          created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
          updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
      );

      -- Opcional, mas boa prática: Trigger para atualizar 'updated_at' automaticamente
      CREATE OR REPLACE FUNCTION trigger_set_timestamp()
      RETURNS TRIGGER AS $$
      BEGIN
        NEW.updated_at = NOW();
        RETURN NEW;
      END;
      $$ LANGUAGE plpgsql;

      -- Remove o trigger se já existir, para evitar erro na recriação
      DROP TRIGGER IF EXISTS set_timestamp ON flags;

      CREATE TRIGGER set_timestamp
      BEFORE UPDATE ON flags
      FOR EACH ROW
      EXECUTE PROCEDURE trigger_set_timestamp();
  job:
    name: flag-db-init
    image: 654654467270.dkr.ecr.us-east-1.amazonaws.com/flag-service:e8c9342
    command: ["sh", "-c"]
    args:
      - |
        echo "Waiting for database..."
        until pg_isready -h "$POSTGRES_HOST" -p "$POSTGRES_PORT" -U "$POSTGRES_USER"; do
          sleep 2
        done

        export PGPASSWORD="$POSTGRES_PASSWORD"


targeting-service:
  namespace: targeting-service
  maxSurge: 50%
  maxUnavailable: 0
  replicas: 2
  image: 654654467270.dkr.ecr.us-east-1.amazonaws.com/targeting-service:c05aa7a
  port: 8003
  ingress: 
    path: /targeting-service(/|$)(.*)


evaluation-service:
  namespace: evaluation-service
  maxSurge: 50%
  maxUnavailable: 0
  replicas: 2
  image: 654654467270.dkr.ecr.us-east-1.amazonaws.com/evaluation-service:2fc0b5b
  port: 8004
  ingress: 
    path: /evaluation-service(/|$)(.*)

analytics-service:
  namespace: analytics-service
  maxSurge: 50%
  maxUnavailable: 0
  replicas: 2
  image: 654654467270.dkr.ecr.us-east-1.amazonaws.com/analytics-service:b45587c
  port: 8005
  ingress: 
    path: /analytics-service(/|$)(.*)
  hpa:
    name: analytics-hpa
    minReplicas:
    maxReplicas:
    targetCPU:

argocd:
  namespace: argocd
  port: 80
  ingress:
    path: /argocd(/|$)(.*)