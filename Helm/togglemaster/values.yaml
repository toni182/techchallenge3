auth-service:
  namespace: auth-service
  maxSurge: 50%
  maxUnavailable: 0
  replicas: 2
  image: 654654467270.dkr.ecr.us-east-1.amazonaws.com/auth-service:05a5aa8
  port: 8001
  ingress: 
    path: /auth-service(/|$)(.*)
  configMap:
    name: db-auth-service-init-sql
    init.sql: |
      CREATE TABLE IF NOT EXISTS api_keys (
        id SERIAL PRIMARY KEY,
        name VARCHAR(100) NOT NULL,
        
        -- key_hash armazena o hash SHA-256 da chave, que tem 64 caracteres hexadecimais
        key_hash VARCHAR(64) NOT NULL UNIQUE, 
        
        is_active BOOLEAN DEFAULT true,
        created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
      );
  job:
    name: auth-db-init
    image: 654654467270.dkr.ecr.us-east-1.amazonaws.com/auth-service:05a5aa8
    command: ["sh", "-c"]
    args:
      - |
        echo "Waiting for database..."
        until pg_isready -h "$POSTGRES_HOST" -p "$POSTGRES_PORT" -U "$POSTGRES_USER"; do
          sleep 2
        done

        export PGPASSWORD="$POSTGRES_PASSWORD"

        echo "Creating database if not exists..."
        PGPASSWORD="$POSTGRES_PASSWORD" psql \
          -h "$POSTGRES_HOST" \
          -p "$POSTGRES_PORT" \
          -U "$POSTGRES_USER" \
          -d postgres \
          -c "CREATE DATABASE IF NOT EXISTS $POSTGRES_DB;"

        echo "Running init.sql..."
        PGPASSWORD="$POSTGRES_PASSWORD" psql \
          -h "$POSTGRES_HOST" \
          -p "$POSTGRES_PORT" \
          -U "$POSTGRES_USER" \
          -d "$POSTGRES_DB" \
          -f /sql/init.sql
        echo "Table created successfully"
        echo "--------"
        echo "Checking tables..."
        psql \
          -h $POSTGRES_HOST \
          -p $POSTGRES_PORT \
          -U $POSTGRES_USER \
          -d $POSTGRES_DB \
          -c "\dt"

        echo "Job ended"

flag-service:
  namespace: flag-service
  maxSurge: 50%
  maxUnavailable: 0
  replicas: 2
  image: 654654467270.dkr.ecr.us-east-1.amazonaws.com/flag-service:66820e6
  port: 8002
  ingress: 
    path: /flag-service(/|$)(.*)
  configMap:
    name: db-flag-service-init-sql
    init.sql: |
      CREATE TABLE IF NOT EXISTS flags (
          id SERIAL PRIMARY KEY,

          -- 'name' é a chave de negócio única (ex: 'enable-new-checkout')
          name VARCHAR(100) UNIQUE NOT NULL, 
          
          description TEXT,
          
          -- Este é o 'kill switch' global. Se for false, a flag está desativada para todos.
          is_enabled BOOLEAN NOT NULL DEFAULT false,
          
          created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
          updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
      );

      -- Opcional, mas boa prática: Trigger para atualizar 'updated_at' automaticamente
      CREATE OR REPLACE FUNCTION trigger_set_timestamp()
      RETURNS TRIGGER AS $$
      BEGIN
        NEW.updated_at = NOW();
        RETURN NEW;
      END;
      $$ LANGUAGE plpgsql;

      -- Remove o trigger se já existir, para evitar erro na recriação
      DROP TRIGGER IF EXISTS set_timestamp ON flags;

      CREATE TRIGGER set_timestamp
      BEFORE UPDATE ON flags
      FOR EACH ROW
      EXECUTE PROCEDURE trigger_set_timestamp();
  job:
    name: flag-db-init
    image: 654654467270.dkr.ecr.us-east-1.amazonaws.com/flag-service:66820e6
    command: ["sh", "-c"]
    args:
      - |
        echo "Waiting for database..."
        until pg_isready -h "$POSTGRES_HOST" -p "$POSTGRES_PORT" -U "$POSTGRES_USER"; do
          sleep 2
        done

        export PGPASSWORD="$POSTGRES_PASSWORD"

        echo "Running init.sql..."
        PGPASSWORD="$POSTGRES_PASSWORD" psql \
          -h "$POSTGRES_HOST" \
          -p "$POSTGRES_PORT" \
          -U "$POSTGRES_USER" \
          -d "$POSTGRES_DB" \
          -f /sql/init.sql
        echo "Table created successfully"
        echo "--------"
        echo "Checking tables..."
        psql \
          -h $POSTGRES_HOST \
          -p $POSTGRES_PORT \
          -U $POSTGRES_USER \
          -d $POSTGRES_DB \
          -c "\dt"

        echo "Job ended"

targeting-service:
  namespace: targeting-service
  maxSurge: 50%
  maxUnavailable: 0
  replicas: 2
  image: 654654467270.dkr.ecr.us-east-1.amazonaws.com/targeting-service:2d5a5fa
  port: 8003
  ingress: 
    path: /targeting-service(/|$)(.*)
  configMap:
    name: db-targeting-service-init-sql
    init.sql: |
      CREATE TABLE IF NOT EXISTS targeting_rules (
          id SERIAL PRIMARY KEY,

          -- 'flag_name' é a chave de negócio única. 
          -- Cada flag pode ter no máximo UMA regra de segmentação.
          flag_name VARCHAR(100) UNIQUE NOT NULL,
          
          -- Se a regra de segmentação em si está ativa ou não
          is_enabled BOOLEAN NOT NULL DEFAULT true,
          
          -- Armazena a lógica da regra como um JSON.
          -- Ex: {"type": "PERCENTAGE", "value": 50}
          -- Ex: {"type": "USER_LIST", "values": ["u1", "u2"]}
          rules JSONB NOT NULL,
          
          created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
          updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
      );

      -- Trigger para atualizar 'updated_at' automaticamente (igual ao do flag-service)
      CREATE OR REPLACE FUNCTION trigger_set_timestamp()
      RETURNS TRIGGER AS $$
      BEGIN
        NEW.updated_at = NOW();
        RETURN NEW;
      END;
      $$ LANGUAGE plpgsql;

      DROP TRIGGER IF EXISTS set_timestamp ON targeting_rules;

      CREATE TRIGGER set_timestamp
      BEFORE UPDATE ON targeting_rules
      FOR EACH ROW
      EXECUTE PROCEDURE trigger_set_timestamp();
  job:
    name: targeting-db-init
    image: 654654467270.dkr.ecr.us-east-1.amazonaws.com/targeting-service:2d5a5fa
    command: ["sh", "-c"]
    args:
      - |
        echo "Waiting for database..."
        until pg_isready -h "$POSTGRES_HOST" -p "$POSTGRES_PORT" -U "$POSTGRES_USER"; do
          sleep 2
        done

        export PGPASSWORD="$POSTGRES_PASSWORD"

        echo "Running init.sql..."
        PGPASSWORD="$POSTGRES_PASSWORD" psql \
          -h "$POSTGRES_HOST" \
          -p "$POSTGRES_PORT" \
          -U "$POSTGRES_USER" \
          -d "$POSTGRES_DB" \
          -f /sql/init.sql
        echo "Table created successfully"
        echo "--------"
        echo "Checking tables..."
        psql \
          -h $POSTGRES_HOST \
          -p $POSTGRES_PORT \
          -U $POSTGRES_USER \
          -d $POSTGRES_DB \
          -c "\dt"

        echo "Job ended"

evaluation-service:
  namespace: evaluation-service
  maxSurge: 50%
  maxUnavailable: 0
  replicas: 2
  image: 654654467270.dkr.ecr.us-east-1.amazonaws.com/evaluation-service:b74a948
  port: 8004
  ingress: 
    path: /evaluation-service(/|$)(.*)

analytics-service:
  namespace: analytics-service
  maxSurge: 50%
  maxUnavailable: 0
  replicas: 2
  image: 654654467270.dkr.ecr.us-east-1.amazonaws.com/analytics-service:9746080
  port: 8005
  ingress: 
    path: /analytics-service(/|$)(.*)
  hpa:
    name: analytics-hpa
    minReplicas:
    maxReplicas:
    targetCPU:

argocd:
  namespace: argocd
  port: 80
  ingress:
    path: /argocd